import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '02-componentes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
